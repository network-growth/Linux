# 🔒 [Container Supply Chain Security] 공급망 보안 고도화 프로젝트 3일차 - Shell Script 기반 보안 파이프라인(SecOps) 자동화 구축 (2026-06-19)

## 1. 대규모 프로젝트 시나리오
당신은 대규모 가상 핀테크(금융 결제) 기업인 'Next-Pay'의 Lead Infrastructure Architect입니다. 1일차에 글로벌 표준 이미지 취약점 스캔 엔진인 **Trivy**를 하부 OS에 수동 구축하고, 베이스 이미지를 정밀 타격하여 숨겨진 보안 결함을 사전에 발본색원하는 관제 기지를 가동했습니다. 또한 2일차에는 **Cosign** 비대칭 암호화 키페어를 활용하여 폐쇄망 환경에서의 이미지 무결성 검증 체계를 수립했습니다.

그러나 사내 공용 테스트 서버 가동 후, 인프라 하부에 또 다른 운영 병목 현상이 포착되었습니다. 개발자들로부터 "배포 단계마다 매번 수동으로 스캔 명령어를 치고, tar 파일을 인출하고, Cosign 패스워드를 타이핑하느라 릴리즈 속도가 심각하게 저하된다"는 불만이 폭주한 것입니다. 심지어 몇몇 작업자는 배포 일정이 촉박하다는 이유로 이 필수 보안 검증 절차를 임의로 누락한 채 컨테이너를 클러스터에 바로 주입하려는 위험한 움직임까지 감지되었습니다.

컨테이너의 '이름'과 '태그'만 신뢰하고 클러스터에 배포하는 방식은 제로 트러스트 보안 철학에 위배됩니다. 이에 경영진은 인간의 실수(Human Error)를 원천 차단하고 제로 트러스트 보안 철학을 빌드 파이프라인 전반에 강제하기 위해서는, 아키텍트가 정립한 일련의 검증 및 서명 프로세스가 단 한 번의 타격으로 완벽하게 제어되는 **보안 자동화 파이프라인(SecOps Pipeline)** 구축이 시급합니다.

### 프로젝트 미션
- 로컬 인프라 및 빌드 환경을 위해 전체 보안 검증 프로세스를 일사천리로 수행하는 영문 표준 자동화 쉘 스크립트(`secure_build.sh`)를 작성한다.
- 위험도가 높은 결함(`HIGH` 등급 이상)이 발견되면 후속 서명 프로세스를 즉시 차단하고 파이프라인을 강제 종료시키는 **Fail-Fast 방어 정책**을 수립한다.

---

## 2. 실습 구현 기록: 영문 표준 SecOps 자동화 스크립트 설계 및 안착

### [Task 1] 나노(nano) 편집기를 활용한 자동화 쉘 스크립트 생성
* 명령어:
  nano secure_build.sh
* 설정의 뜻:
  가상머신 커널 내부의 텍스트 편집기 세션을 열어 Next-Pay의 보안 파이프라인을 통제할 쉘 스크립트 파일 인프라를 생성합니다. 터미널 환경에서의 로그 깨짐 및 전역 호환성을 고려하여 모든 출력(Log) 메시지와 주석은 영문 표준(POSIX 스타일)으로 엄격하게 구성 및 타이핑합니다.

### [Task 2] 쉘 스크립트 파일 편집기 내부 소스 코드 주입 및 저장
* 설정의 뜻:
  단 한 번의 스크립트 가동으로 전 과정이 무인 제어되도록 소스코드를 주입합니다. 특히 상단에 명시한 `set -e` 옵션은 스크립트 내부 명령어가 에러를 반환할 시 후속 처리를 즉시 중단시키며, Trivy 스캔 구역의 `--exit-code 1` 옵션과 연계하여 위험 요소 발견 시 서명 단계를 완전 블로킹하는 강력한 SecOps 방어막 역할을 수행합니다.
* 실습 증명 사진:

<img width="739" height="606" alt="Image" src="https://github.com/user-attachments/assets/b2d5693b-0064-4167-87dd-31d621930ee0" />

---

## 3. 대규모 엔지니어링 억까 연쇄 폭발 및 디버깅 로그 (Troubleshooting)

정순 구조로 자동화 스크립트를 최초 기동 및 커스텀하는 과정에서 리눅스 파일 시스템 및 변수 규격 억까가 발생했으나, 로그 추적을 통해 이를 완벽하게 분쇄했습니다.

### 🚨 억까 1: 하이픈/언더바 파일명 오차로 인한 커널 접근 거부 에러
* 현상:
  나노 편집기로 `secure_build.sh`(언더바) 파일을 생성했으나, 실행 권한을 부여하는 과정에서 하이픈을 사용하여 `chmod +x secure-build.sh`를 타격함에 따라 리눅스 커널이 `No such file or directory` 에러를 유발함.
* 조치:
  쉘 시스템이 참조하는 파일 주소의 구조적 일관성을 확보하기 위해 명시적으로 파일 이름을 일치시켜 `chmod +x secure_build.sh` 명령어를 정확히 재타격하여 즉각 해결함.
* 실습 증명 사진:

<img width="628" height="93" alt="스크린샷 2026-06-19 193655" src="https://github.com/user-attachments/assets/7116bcdb-e864-4c11-941e-0fea1bb5d28d" />


### 🚨 억까 2: 변수 선언 문법 오류(`command not found`) 규격 억까
* 현상:
  스크립트 실행 시 7번째 라인에서 `SIG-FILE=python-sign.sig: command not found` 에러와 함께 파이프라인이 크래시됨.
* 조치:
  리눅스 쉘 스크립트 환경에서는 변수명에 하이픈(`-`)이 포함될 경우 독립된 명령어로 오인하는 특성이 있음을 식별. `SIG-FILE`을 표준 변수 규격인 언더바 표기법(`SIG_FILE`)으로 리팩토링하여 문법 규격 오류를 완벽하게 파쇄함.
* 실습 증명 사진:

<img width="1288" height="217" alt="스크린샷 2026-06-19 193906" src="https://github.com/user-attachments/assets/ebf4d958-c7da-46d6-866a-a7d9aac22c06" />


### 🚨 억까 3: 의도된 Fail-Fast 보안 차단 로직의 실전 증명 완료
* 현상:
  대상 이미지를 위험 결함이 잔존하는 `python:3.9-alpine`으로 지정하고 가동했을 때, 2단계 Trivy 스캔 구간에서 `HIGH` 등급 취약점 테이블들을 검출해낸 뒤, 후속 3, 4, 5단계 서명 구역으로 진입하지 못하고 전체 스크립트가 강제 중단됨.
* 조치:
  이는 시스템 에러가 아닌, 설계한 `--exit-code 1` 자물쇠가 오염된 이미지의 확산을 감지하고 완벽하게 배포를 블로킹해낸 'SecOps 자동화 파이프라인의 성공적인 방어 동작'임을 로그 역추적을 통해 최종 검증하고 신뢰도가 보장된 순수 이미지로 교체를 단행함.
* 실습 증명 사진:
  
<img width="1280" height="806" alt="스크린샷 2026-06-19 194035" src="https://github.com/user-attachments/assets/18f67e00-30ef-41f7-a59c-59e5576aa258" />

---

## 4. 최종 성공 구현 스크립트 (Pure Offline Sign & Verify)

모든 원격 의존성과 파이프라인 규격 오차를 제거한 최종 성공 자동화 스크립트 프로세스입니다.

* 명령어:
  chmod +x secure_build.sh
* 설정의 뜻:
  리눅스 OS 커널이 해당 쉘 스크립트 자원을 바이너리 프로그램처럼 즉시 가동하고 제어할 수 있도록 완전한 실행 권한 플래그를 할당합니다.

* 명령어:
  export COSIGN_PASSWORD="YOUR_ARCHITECT_PASSWORD"
* 설정의 뜻:
  인간의 개입을 완전히 배제하는 무인 자동화를 위해 Cosign 패스워드를 환경 변수(`export`)로 메모리에 미리 주입합니다.

* 명령어:
  ./secure_build.sh
* 설정의 뜻:
  보안 차단 정책 확인 후 취약점이 없는 `alpine:latest`로 타겟을 전환하여 자동화 파이프라인 엔진을 최종 가동합니다.
* 설정의 결과:
  Trivy 취약점 수치 `0`개 확인과 동시에 자동으로 락이 해제되었으며, 멈춤 없이 질주하여 4단계 서명 생성 및 5단계 오프라인 교차 무결성 검증까지 단 5초 만에 다이렉트로 관통했습니다. 터미널 화면에 암호학적 위·변조가 없음을 보증하는 **영롱한 `Verified OK` 사인과 최종 `[COMPLETE]` 메시지**를 출력하며 3일차 공급망 보안 자동화 미션을 완벽하게 완수합니다.
* 실습 증명 사진:

<img width="1276" height="781" alt="Image" src="https://github.com/user-attachments/assets/ebb78552-6d87-489a-9458-df8b0b54a3b1" />
