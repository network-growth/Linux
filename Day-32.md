# 🎙️ 한국항공대학교 학생부종합/특기자 전형 면접 대비 예상 질문 & 답변 스크립트

## 💬 Q1. "Istio 서비스 메쉬를 써서 제로 트러스트를 구축했다고 했는데, 고등학생이 하기에는 너무 거창한 주제 아닌가요? 정확히 뭘 한 건지 핵심만 설명해 보세요."

> **💡 면접 답변의 정석 (두괄식 + 핵심 아키텍처 관통)**
>
> "네, 제가 구축한 프로젝트의 핵심 미션은 **'내부망에 침입한 해커가 패킷을 스니핑하더라도 결제 데이터가 절대 유출되지 않는 핀테크 인프라'**를 만드는 것이었습니다. 
> 
> 이를 위해 기존의 외부 방화벽만 믿는 경계 보안을 넘어, 내부망의 그 어떤 서비스도 신뢰하지 않는 **'제로 트러스트(Zero Trust)'** 모델을 도입했습니다. 핵심 구현은 두 가지입니다.
> 
> 첫째, **`peer-auth.yaml`** 정책을 통해 서비스 메쉬 내부의 모든 Pod 간 통신에 **mTLS STRICT(상호 TLS 암호화)**를 강제했습니다. 이로 인해 모든 트래픽이 평문이 아닌 암호화 터널을 타게 되며, 실제로 통신 패킷 내부에 Istio가 주입한 암호화 인증서 헤더인 **`X-Forwarded-Client-Cert`**가 정상적으로 각인되는 것을 수동 확인했습니다.
> 
> 둘째, **`auth-policy.yaml`**을 수립하여 인가된 프론트엔드(`sa/sleep`)가 지정된 엔드포인트(`GET /headers`)로 쏘는 트래픽만 허용하는 **L7 화이트리스트 방화벽**을 구축했습니다. 인가되지 않은 임시 컨테이너나 외부 가상머신 호스트가 백엔드 가상 IP로 우회 침투를 시도할 때, 프록시 선에서 즉시 세션을 파괴하고 **`403 Forbidden(RBAC: access denied)`**으로 철벽 방어하는 아키텍처를 완성했습니다."

---

## 💬 Q2. "실습하면서 가장 기억에 남는 트러블슈팅(문제 해결) 경험이 있다면 수학적/논리적으로 어떻게 풀었나요?"
*(★ 수학 점수가 낮다는 우려를 **"인프라의 논리적 디버깅 역량"**으로 완벽하게 방어하는 가장 중요한 구간입니다.)*

> **💡 면접 답변의 정석 (이슈 정의 ➡️ 원인 분석 ➡️ 해결 방안)**
>
> "네, 인프라를 구축하며 총 세 번의 거대한 네트워크 및 컴포넌트 억까(장애)를 마주쳤고, 이를 **단계별 로그 분석이라는 엔지니어링 접근법**으로 해결했습니다.
> 
> 가장 기억에 남는 것은 **'쿠버네티스 디플로이먼트 리스타트에 따른 Pod 생명 주기(Lifecycle) 추적'** 이슈였습니다. 정책 반영을 위해 `rollout restart`를 실행한 직후, 기존 Pod 명칭으로 통신을 찌르자 `NotFound` 에러가 발생했습니다. 
> 
> 원인을 논리적으로 추적해 보니, 쿠버네티스는 컨테이너 새로고침 시 기존 객체를 그대로 두는 것이 아니라 '헌 집을 허물고 새 집을 짓는' 롤링 업데이트 방식을 취하므로, Pod 이름의 해시코드가 실시간으로 완전히 유동 변화한다는 구조적 특성을 깨달았습니다.
> 
> 이를 해결하기 위해 Pod 명칭을 하드코딩하여 명령어를 날리는 방식을 버리고, 쿠버네티스 내부 사령부 API에 직접 질의하여 현재 실시간으로 살아있는 Pod 명칭을 가로채는 **`jsonpath` 매핑 기법(`{.items[0].metadata.name}`)을 스크립트에 결합**하여 실시간 추적 자동화를 통해 통신 검증을 성공시켰습니다. 
> 
> 수식으로 채워진 수학 점수는 조금 부족했을지 몰라도, **네트워크 레이어의 통신 에러 코드를 분석하고 시스템 아키텍처의 인프라 메커니즘을 논리적으로 역추적하여 문제를 해결하는 스템(STEM) 역량**만큼은 그 누구보다 깊다고 자부합니다."

---

## 💬 Q3. "앞으로 우리 한국항공대학교에 입학해서 더 발전시키고 싶은 연구 방향이 있습니까?"
*(★ 새로 선택한 '후보 1번 공급망 보안' 프로젝트를 입학 후 학업 계획과 자연스럽게 엮는 고도의 전략입니다.)*

> **💡 면접 답변의 정석 (발전 가능성 + 학업 계획 연계)**
>
> "네, 이번 프로젝트를 통해 컨테이너 내부와 서비스 메쉬 간의 '통신 보안(mTLS)'은 완벽하게 제어할 수 있게 되었습니다. 하지만 인프라 아키텍트로서 한 단계 더 나아가 **'컨테이너 공급망 보안(Container Supply Chain Security)'** 영역으로 고도화하고 싶습니다.
> 
> 아무리 서비스 메쉬 방화벽이 튼튼해도, 빌드 시스템 자체가 해커에게 오염되어 악성코드가 주입된 도커 이미지가 클러스터에 배포된다면 무용지물이 되기 때문입니다.
> 
> 한국항공대학교에 입학하여 컴퓨터공학의 깊이 있는 소프트웨어 엔지니어링 지식을 쌓고, 이를 바탕으로 **Trivy**를 통한 이미지 빌드 타임 취약점 스캔 파이프라인 구축, **Cosign** 기반의 이미지 디지털 서명 메커니즘, 그리고 쿠버네티스 입구에서 서명이 없는 컨테이너의 배포 자체를 원천 차단하는 **Kyverno 준수 제어(Admission Controller)** 시스템을 연구하고 싶습니다. 
> 
> 1-2일차에 완성한 제로 트러스트 통신망에 공급망 보안까지 결합하여, 대한민국 금융 인프라를 안전하게 수호하는 전천후 리드 인프라 아키텍트가 되는 것이 제 최종 목표입니다."
